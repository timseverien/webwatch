{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vitest run && npm run build --workspaces --if-present",
    "build:website": "vitest run && npm run build --workspace=@ww/website",
    "dev": "npm run dev --workspaces --if-present",
    "test": "vitest run && npm run test --workspaces --if-present",
    "tokens": "npm run run --workspace=@ww/token-generator",
    "update:data": "npm run build --workspace=@ww/data"
  },
  "workspaces": [
    "packages/core",
    "packages/data",
    "packages/token-generator",
    "packages/website"
  ],
  "devDependencies": {
    "@types/node": "^20.3.3",
    "vite-node": "^0.32.2",
    "vitest": "^0.33.0"
  },
  "dependencies": {
    "date-fns": "^2.30.0",
    "slugify": "^1.6.6"
  }
}