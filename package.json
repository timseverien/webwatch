{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present"
  },
  "workspaces": [
    "packages/core",
    "packages/website"
  ],
  "devDependencies": {
    "vite-node": "^0.32.2"
  }
}